spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workers
    username: postgres
    password: qazplm
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: '1.0.0'
    locations: classpath:db/migration
server:
  port: 8082

online-shop:
  service:
    change-status-url: http://localhost:8080/home/order/changestatus
    change-product-quantity-url: http://localhost:8080/home/order/changeproductquantity